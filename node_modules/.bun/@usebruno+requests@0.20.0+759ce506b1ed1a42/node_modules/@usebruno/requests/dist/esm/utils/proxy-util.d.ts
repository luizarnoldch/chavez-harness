/**
 * Transform proxy config from old format to new format.
 * Old format: { enabled: true | false | 'global', protocol, hostname, port, auth: { enabled, ... }, ... }
 * New format: { disabled?, inherit, config: { protocol, hostname, port, auth: { disabled?, ... }, ... } }
 */
interface OldProxyAuth {
    enabled?: boolean;
    username?: string;
    password?: string;
}
interface OldProxyConfig {
    enabled?: true | false | 'global';
    protocol?: string;
    hostname?: string;
    port?: number | null;
    auth?: OldProxyAuth;
    bypassProxy?: string;
}
interface NewProxyAuth {
    disabled?: boolean;
    username?: string;
    password?: string;
}
interface NewProxyConfig {
    disabled?: boolean;
    inherit: boolean;
    config: {
        protocol: string;
        hostname: string;
        port: number | null;
        auth: NewProxyAuth;
        bypassProxy: string;
    };
}
export declare const transformProxyConfig: (proxy: OldProxyConfig | NewProxyConfig | null | undefined) => NewProxyConfig | OldProxyConfig;
export {};
