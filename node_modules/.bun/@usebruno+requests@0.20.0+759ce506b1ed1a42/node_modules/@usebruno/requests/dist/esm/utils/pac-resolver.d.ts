type TlsOptions = {
    ca?: string | string[];
    rejectUnauthorized?: boolean;
    minVersion?: string;
};
export type PacWrapper = {
    resolve: (url: string) => Promise<string[]>;
};
export type GetPacResolverParams = {
    pacSource: string;
    httpsAgentRequestFields?: TlsOptions;
    opts?: {
        cacheTtlMs?: number;
        timeoutMs?: number;
    };
};
export declare function getPacResolver({ pacSource, httpsAgentRequestFields, opts }: GetPacResolverParams): Promise<PacWrapper>;
export declare function clearPacCache(keyPrefix?: string): void;
export declare const _CACHE: Map<string, {
    wrapper: Promise<PacWrapper>;
    ts: number;
}>;
export {};
