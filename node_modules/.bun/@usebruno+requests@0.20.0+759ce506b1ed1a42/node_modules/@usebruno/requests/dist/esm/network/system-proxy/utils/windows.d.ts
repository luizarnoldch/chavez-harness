import { ProxyConfiguration, ProxyResolver } from '../types';
export declare class WindowsProxyResolver implements ProxyResolver {
    detect(opts?: {
        timeoutMs?: number;
    }): Promise<ProxyConfiguration>;
    private getInternetOptions;
    private getWinHttpProxy;
    private getSystemProxyEnvironment;
    private getUserEnvironmentProxy;
    private parseProxyString;
}
