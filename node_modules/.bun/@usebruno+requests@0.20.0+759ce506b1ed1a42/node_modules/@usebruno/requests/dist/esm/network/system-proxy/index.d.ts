import { ProxyConfiguration, SystemProxyResolverOptions } from './types';
export declare class SystemProxyResolver {
    private osPlatform;
    private commandTimeoutMs;
    private activeDetection;
    constructor(options?: SystemProxyResolverOptions);
    getSystemProxy(): Promise<ProxyConfiguration>;
    private detectSystemProxy;
    private detectByPlatform;
    getEnvironmentVariables(): ProxyConfiguration;
}
export declare function getSystemProxy(): Promise<ProxyConfiguration>;
export { ProxyConfiguration } from './types';
