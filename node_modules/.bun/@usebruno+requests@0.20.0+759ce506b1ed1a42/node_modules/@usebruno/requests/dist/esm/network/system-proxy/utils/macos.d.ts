import { ProxyConfiguration, ProxyResolver } from '../types';
export declare class MacOSProxyResolver implements ProxyResolver {
    detect(opts?: {
        timeoutMs?: number;
    }): Promise<ProxyConfiguration>;
    private parseScutilOutput;
    private parseConfiguration;
    private buildProxyConfiguration;
}
