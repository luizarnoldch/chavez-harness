import { ProxyConfiguration, ProxyResolver } from '../types';
export declare class LinuxProxyResolver implements ProxyResolver {
    detect(opts?: {
        timeoutMs?: number;
    }): Promise<ProxyConfiguration>;
    private getGSettingsProxy;
    private getKDEProxy;
    private getEnvironmentFileProxy;
    private getSystemdProxy;
    private parseProxyFromContent;
}
