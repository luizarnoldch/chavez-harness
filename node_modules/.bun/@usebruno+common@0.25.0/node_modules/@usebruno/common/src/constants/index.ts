export const DEFAULT_HTTP_ITEM_SETTINGS = {
  encodeUrl: true,
  forwardAuthorizationHeader: false
};

// Documentation URLs shared by the app and the CLI. Keep them here so both
// packages surface the same links.
export type DocsUrlKey = 'secret-managers' | 'secret-manager-migration' | 'documentation';

export const DOCS_URLS: Record<DocsUrlKey, string> = {
  'secret-managers': 'https://docs.usebruno.com/secrets-management/secret-managers/overview',
  'secret-manager-migration': 'https://docs.usebruno.com/secrets-management/migration',
  'documentation': 'https://docs.usebruno.com'
};
