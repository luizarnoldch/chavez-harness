# Dependencies

| Package | Kind | Install |
|---------|------|---------|
| `cypress` | devDependency | `bun add -d cypress` |

Encoded in `scripts/install-deps.sh`. Do not invent alternate install commands.
