meta {
  name: Get session
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/sessions/{{sessionId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
}
