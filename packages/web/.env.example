# Browser talks same-origin to Astro (:4321). Astro proxies /api and /ws to the API.
# Leave PUBLIC_API_URL empty unless you intentionally bypass the proxy (cross-origin).
# PUBLIC_API_URL=

# Server-side upstream for the proxy (Vite / Node serve). Not exposed to the browser.
API_INTERNAL_URL=http://localhost:28001
