import {
  DEFAULT_CHAT_MODEL,
  DEFAULT_CHAT_PROVIDER,
} from "@chavez-harness/shared";

/** Local echo mock model (explicit opt-in). */
export const LOCAL_MODEL = "eco";

export const DEFAULT_SESSION_MODEL = DEFAULT_CHAT_MODEL;
export const DEFAULT_SESSION_PROVIDER = DEFAULT_CHAT_PROVIDER;
